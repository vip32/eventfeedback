import { RouterConfig } from '@angular/router';

import { EventListComponent } from './index';

export const EventListRoutes: RouterConfig = [
  {
    path: 'events',
    component: EventListComponent
  },
];
